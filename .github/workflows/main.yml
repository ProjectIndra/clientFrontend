name: Run script for UI deployment

on:
  push:
    branches:
      - feature/uiux-improvment

jobs:
  deploy:
    runs-on: avinash
    steps:
      - name: Run the UI deployment script
        run: |
          cd /home/avinash/scripts
          sudo bash clientFrontendScript.sh
